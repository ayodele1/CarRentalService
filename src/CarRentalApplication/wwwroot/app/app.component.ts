import { Component } from '@angular/core';

@Component({
  selector: 'ng-application',
  template: `
        <reservationsList></reservationsList>
    `
})
export class AppComponent  { name = 'Angular'; }
